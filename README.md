# techblog_server_v2
Nodejs &amp; React build by Skirmish and tungnt

# install package
`npm install`

# create environment
`cp .env-example .env`

# Run
`node server.js`

# Run with docker-compose
`docker-compose up -d`

